{
  "name": "react-wasm-compiler",
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based React compiler using WASM (esbuild-wasm and SWC)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
