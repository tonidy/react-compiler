{
  "name": "reactcompile",
  "version": "0.1.0",
  "type": "module",
  "description": "Compile React apps in the browser with WASM compilers (esbuild-wasm and SWC)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
